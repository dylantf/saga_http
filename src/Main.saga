module Main

import Std.Actor (beam_actor)
import SagaHttp.Http (serve, default_config, text, Request, Response)

fun handle : Request -> Response
handle req = case req.body {
  Just body -> text 200 $"Got body: {body}"
  Nothing -> text 200 $"Hello, you requested {req.path}"
}

main () = {
  serve default_config handle
} with beam_actor
